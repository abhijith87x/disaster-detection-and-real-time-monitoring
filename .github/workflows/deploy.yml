name: CI/CD Deploy

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10' 
      
      # - name: Install dependencies
      #   run: pip install -r requirements.txt
      
      # # - name: Run tests
      # #   run: pytest tests
      # #   env:
      # #     PYTHONPATH: .

      # - name: Create .env
      #   run: |
      #     echo "GOOGLE_CLIENT_ID=${{ secrets.CLIENT_ID }}" >> .env
      #     echo "GOOGLE_CLIENT_SECRET=${{ secrets.CLIENT_SECRET }}" >> .env
      #     echo "ALGORITHM=${{ secrets.ALGORITHM }}" >> .env
      #     echo "ACCESS_TOKEN_EXPIRE_MINUTES=${{ secrets.TOKEN_EXPIRE_MINUTES }}" >> .env
      #     echo "AWS_ACCESS_KEY_ID=${{ secrets.AWS_ACCESS_KEY_ID }}" >> .env
      #     echo "AWS_SECRET_ACCESS_KEY=${{ secrets.AWS_SECRET_ACCESS_KEY }}" >> .env
      #     echo "AWS_REGION=${{ secrets.AWS_REGION }}" >> .env
      #     echo "AWS_BUCKET_NAME=${{ secrets.AWS_BUCKET_NAME }}" >> .env
      #     echo "GOOGLE_GEMINI_KEY=${{ secrets.GOOGLE_GEMINI_KEY }}" >> .env
      # - name: Setup Node.js
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: 22

      # - name: Build React
      #   run: |
      #     cd frontend
      #     npm ci
      #     npm run build    

      # - name: Start containers
      #   run: |
      #     # docker compose down
      #     # docker compose up -d --build
      #     docker compose down
      #     docker compose build --no-cache backend
      #     docker compose up -d

      
      # - name: Check memory
      #   run: |
      #     free -h
      #     docker stats --no-stream || true
      
      # # - name: Debug JWT
      # #   run: |
      # #     docker compose exec -T backend sh -c "ls -la /app/jwt"
      # #     docker compose exec -T backend python -c "import os; print(os.path.exists('/app/jwt/jwt_handler.py'))"
      # #     docker compose exec -T backend python -c "import jwt; print(jwt); print(list(jwt.__path__))"
      # #     docker compose exec -T backend python -c "import auth_jwt.jwt_handler; print('JWT IMPORT OK')"
      
      # - name: Debug JWT
      #   run: |
      #     docker compose exec -T backend sh -c "ls -la /app/auth_jwt"
      #     docker compose exec -T backend python -c "import os; print(os.path.exists('/app/auth_jwt/jwt_handler.py'))"
      #     docker compose exec -T backend python -c "import auth_jwt.jwt_handler; print('JWT IMPORT OK')"

      # - name: Run tests
      #   run: docker compose exec -T backend bash -c "PYTHONPATH=/app pytest tests -v"
      
      # - name: Check container after tests
      #   if: always()
      #   run: |
      #     docker inspect disaster-api \
      #       --format='Memory={{.HostConfig.Memory}} MemorySwap={{.HostConfig.MemorySwap}} OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}}'
      #     docker compose logs backend --tail=100
      # - name: Debug backend failure
      #   run: |
      #     docker inspect disaster-api \
      #       --format='Memory={{.HostConfig.Memory}} MemorySwap={{.HostConfig.MemorySwap}} OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}}'

      #     docker compose logs backend --tail=100
      # - name: Stop containers
      #   if: always()
      #   run: docker compose down
        
  deploy:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # - name: Set up Docker
      #   uses: docker/setup-buildx-action@v2

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
            node-version: 22

      - name: Build React
        run: |
          cd frontend
          npm ci
          npm run build
        
      - name: Build and push Docker image
        run: |
          git pull origin main
          docker build -t abhijith87b/disaster-api:latest .
          docker push abhijith87b/disaster-api:latest

      - name: Deploy to server
        uses: appleboy/ssh-action@v0.1.5
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            cd ~/disaster-detection-and-real-time-monitoring

            echo "GOOGLE_CLIENT_ID=${{ secrets.CLIENT_ID }}" >> .env
            echo "GOOGLE_CLIENT_SECRET=${{ secrets.CLIENT_SECRET }}" >> .env 
            echo "ALGORITHM=${{ secrets.ALGORITHM }}" >> .env
            echo "ACCESS_TOKEN_EXPIRE_MINUTES=${{ secrets.TOKEN_EXPIRE_MINUTES }}" >> .env
            echo "AWS_ACCESS_KEY_ID=${{ secrets.AWS_ACCESS_KEY_ID }}" >> .env
            echo "AWS_SECRET_ACCESS_KEY=${{ secrets.AWS_SECRET_ACCESS_KEY }}" >> .env
            echo "AWS_REGION=${{ secrets.AWS_REGION }}" >> .env
            echo "AWS_BUCKET_NAME=${{ secrets.AWS_BUCKET_NAME }}" >> .env
            echo "GOOGLE_GEMINI_KEY=${{ secrets.GOOGLE_GEMINI_KEY }}" >> .env
            
            git pull origin main
            docker compose pull backend
            docker compose up -d --force-recreate backend 